# --- Local-only tooling (hosts, keys, paths - keep out of GitHub) ---
tools/deploy_to_ha.ps1
tools/deploy*.ps1

# Local reference clone (jskeet/DemoCode - DigiMixer Mackie .NET; not vendored)
reference/digimixer/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.Python

# Environment / secrets (if you add these later)
.env
.env.*

# OS junk
.DS_Store
Thumbs.db

# Wire captures and the show archive pulled off the mixer. Local evidence for
# tools/capture/FINDINGS.md - kept out of git: multi-MB binaries that contain the
# desk's channel names and show data, and this repo is public.
captures/
